#!/bin/sh
#
# $Id: update,v 1.1 2026-07-25 00:35:40+05:30 Cprogrammer Exp mbhangui $
#
case $1 in
	update)
	if [ -f /etc/debian_version ] ; then
		exec sudo env DEBIAN_FRONTEND=noninteractive apt update -y
	elif [ -f /usr/bin/dnf ] ; then
		exec sudo dnf -y update
	fi
	;;
	upgrade)
	if [ -f /etc/debian_version ] ; then
		exec sudo env DEBIAN_FRONTEND=noninteractive apt --allow-downgrades upgrade -y
	else
		exec sudo dnf -y upgrade
	fi
	;;
esac

#
# $Log: update,v $
# Revision 1.1  2026-07-25 00:35:40+05:30  Cprogrammer
# Initial revision
#
#
